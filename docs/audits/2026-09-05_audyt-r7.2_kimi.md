# Raport audytu — RUNDA 7.2 (release-path, potwierdzenie freeze)

**Audytor:** Kimi · **Data:** 2026-09-05 · **Kod:** HEAD `272750da8b748e3faa5a26029e516500f374c883` · `src_tree 4c225639…`
**Uwaga o materiale:** paczki `audyt-r7.2.zip` nie było w załącznikach — weryfikację przeprowadziłem wprost na **bundle gita** (źródło prawdy, lepsze niż zip).

## Weryfikacje mechaniczne (samodzielne)

| Kontrola | Wynik |
|---|---|
| Bundle: HEAD = `272750d` | ✅ zgodny z manifestem |
| `src_tree` / `code_tree` / `math_tree` z HEAD | ✅ `4c225639…` / `7dbf3de4…` / `6fb61151…` — zgodne z manifestem i WYSYLKĄ |
| `src_tree` na commicie evidence `544b12a` | ✅ identyczny — testy 52/52 dotyczą wdrożonego kodu |
| Diff `cf2e1f7..272750d` w `programs/anl_staking/src` | ✅ dokładnie 4 pliki, +69/−1, wyłącznie dodatki (strażniki, bramka mainnet-cfg, stała, wariant błędu na końcu enumu) |
| Historia `cf2e1f7..HEAD` | ✅ liniowa: 3 fixy R7.2 → merge → potem tylko docs/build/skrypty |
| Rebuild SBF / dump on-chain | ⚠️ jak w 7.1 — przyjmuję dowód zespołu (sha `87b431d4…`, slot 185899744, dump 0 bajtów różnicy); sam nie mam toolchain SBF |

## Werdykty na 5 pytań

| # | Werdykt | Uzasadnienie |
|---|---|---|
| 1 (A: testowy Program ID) | **Czyste** | Build bez feature sieciowego ma teraz `ChG81…pcov` — pod `4Cpx…` odbije `DeclaredProgramIdMismatch` na starcie; `anchor build` bez argumentów i `cargo build-sbf` z innego katalogu rozwiązują features z Cargo.toml/CLI (nie z katalogu), więc kombinacja "prod ID bez pinu" nie istnieje. `compile_error!` (mainnet+test-periods, dwie sieci) + 3 strażniki `program_id_*` kompletne. Ścieżki testnet/mainnet nietknięte: `declare_id` dla wariantu bez sieci jest compile-time; w kodzie PDA wyprowadzane z `ctx.program_id` (runtime), strażniki pod `#[cfg(test)]` nie trafiają do SBF. |
| 2 (B: fail-closed pack) | **Czyste** | `pack-audit.sh` teraz wymaga manifestu i binarki oraz porównuje `src_tree`/`code_tree`/`math_tree`/`sha256` z HEAD i lokalnym `.so` — scenariusz "build na A → checkout B → pack" odbija `exit 1` z nazwą rozjętego hasha. **Na mainnet brakuje:** (a) analogicznej procedury dla buildu `network-mainnet` (osobny manifest), (b) niezależnego rebuildu na drugiej maszynie/osobie przed ceremonią (obecnie evidence z jednej maszyny), (c) weryfikacji dump po deployu mainnetowym — jak na testnecie. |
| 3 (C: podaż CAPY 20M) | **Czyste** | Kontrola jednorazowa wystarcza: `mint_authority == None` (check z R5) czyni podaż niezmienną w górę na zawsze; `supply == CAPY_TOTAL_SUPPLY` pod `network-mainnet` domyka tokenomikę WP. Kolejność ceremonii działa w obu wariantach: init przed wypaleniem odbije R5-check `MintHasMintAuthority`; po wypaleniu oba warunki stabilne. Post-init podaż może tylko maleć (burn holderów) — bez wpływu na protokół. Testnet (1B CAPY) bez bramki ✓; nowy błąd 6046 na końcu enumu ✓; strażnik `capy_supply_constant` ✓. |
| 4 (regresja diffu) | **Potwierdzam** | Zweryfikowane `git diff cf2e1f7..272750d -- programs/anl_staking/src`: tylko `lib.rs` (declare_id wariantu testowego + moduł strażników `#[cfg(test)]`), `constants.rs` (stała + strażnik), `errors.rs` (wariant na końcu), `initialize.rs` (jeden `require!` pod `cfg(network-mainnet)`). Ścieżka wykonania testnetu: zero zmian (bramka CAPY skompilowana wyłącznie w buildzie mainnetowym). |
| 5 (freeze) | **TAK** | Patrz werdykt końcowy. |

## Odhaczenie moich punktów z rund 5 i 7

| Punkt | Status |
|---|---|
| R5 Zadanie 1 (jeden HEAD) | ✅ system tree-hashy rozwiązał problem strukturalnie; manifest spójny z HEAD |
| R5 Zadanie 2 (świeży evidence na finalnym HEAD + sha `.so`) | ✅ TEST-LOG na `544b12a`, `src_tree` = deploy; sha `.so` + slot + dump w manifeście |
| R5 Zadanie 3 (Dependabot: dev-dep vs SBF) | ✅ tabela 31 advisories z weryfikacją grafu SBF + `audit.toml`/`deny.toml`; borsh 0.9.3 nieosiągalny (potwierdziłem w 7.1) |
| R7 §5.1 (commit fixów R6-01/02) | ✅ `fe0df6d` + merge `319ad10` |
| R7 §5.2–5.3 (evidence + rebuild + deploy + slot) | ✅ slot 185899744 |
| R7 §5.5 (I-01 pin) | ✅ + wzmocnione w 7.2 (test-ID + strażniki) |
| Otwarte (świadomie post-freeze / ceremonia mainnet) | multisig + `EXPECTED_INIT_AUTHORITY`, ewentualnie nowy Program ID, build `network-mainnet` bez `test-periods`, sweep nadmiaru ANL (moje Info z 7.1 — nadal brak instrukcji; operacyjne, nie blokuje), WP (cooldown, 365 dni, 200M, polityka orphanów), upgrade stosu Solana/Anchor |

## Werdykt końcowy

**FREEZE: TAK** — kod na `src_tree 4c2256398137bb417a1b769316137852d14ec4d5` potwierdzam jako finalny kod testnetowy i bazę mainnetu (z ceremonią: `EXPECTED_INIT_AUTHORITY` → multisig, `CAPY_TOTAL_SUPPLY` aktywne, build `network-mainnet` bez `test-periods`, nowy manifest + dump).

**Ocena: 9/10** (jak w 7.1 — punkt zostaje za sprawy pozakodowe: jeden hot key do czasu multisiga, brak sweep nadmiaru ANL, WP do aktualizacji).

Zastrzeżenie: nie mam lokalnie toolchain SBF — część binarną provenance (rebuild = on-chain) przyjmuję z dowodu zespołu; całą część gitową i kodową zweryfikowałem niezależnie.
