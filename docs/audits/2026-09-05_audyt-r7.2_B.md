# Security Audit Report — ANL Staking Protocol (Round 7.2)

**Data:** 2026-09-05  
**HEAD paczki:** `272750da8b748e3faa5a26029e516500f374c883`  
**src_tree:** `4c2256398137bb417a1b769316137852d14ec4d5`  
**Binarka / slot:** `87b431d4…` / 185899744  
**Zakres:** wyłącznie release-path (A/B/C). Logika XNT/ANL/CAPY entitlement bez zmian vs R7.1.  
**cargo/Anchor:** brak w tym środowisku — weryfikacja statyczna.

---

## Tabela werdyktów

| # | Pytanie | Werdykt |
|---|---|---|
| 1 | A — build bez `network-*` pod produkcyjnym ID bez pinu | **Czyste** |
| 2 | B — `pack-audit.sh` fail-closed | **Czyste** na testnet; **Uwaga** na mainnet (osobny manifest) |
| 3 | C — podaż CAPY 20M przy `init_capy_vault` | **Czyste** (kolejność ceremonii: mint 20M → burn authority → init) |
| 4 | Regresja `src_tree` vs 7.1 | **Czyste** — testnet runtime bez zmiany ścieżki ekonomicznej |
| 5 | Freeze kodu testnetowego `4c225639…` | **TAK** |

**FREEZE: TAK** — `src_tree 4c225639…` może być finalnym kodem testnetu i bazą mainnetu po ceremonii (multisig `EXPECTED_INIT_AUTHORITY`, build `network-mainnet` bez `test-periods`, mint CAPY = `CAPY_TOTAL_SUPPLY`).

**Ocena: 9 / 10** (ścieżka release domknięta; 1 pkt za F-02 / ceremonia, nie za lukę w instrukcjach).

Findingów Critical / High / Medium / Low w tej delcie: **0**.

---

### 1. A — Program ID

`lib.rs` 22–32:

- `network-mainnet` / `network-testnet` → `4Cpxg8U3…` **oraz** pin `EXPECTED_INIT_AUTHORITY`
- brak obu feature → `ChG81WAp…`
- `compile_error!` na `mainnet+test-periods` i dwie sieci naraz — bez zmian

`anchor build` / `cargo-build-sbf` bez feature daje test-ID. Wgranie tej binarki pod `4Cpx…` → `DeclaredProgramIdMismatch`.

Strażniki `program_id_pinned_{testnet,mainnet}` (`== PROD && ≠ TEST`) i `program_id_is_test_id` (`== TEST && ≠ PROD`).

Ścieżka testnet/mainnet: `declare_id` nadal `4Cpx…`. PDA i `find_program_address(..., program_id)` biorą ID z **wdrożonej** binarki. Seeds nie zmieniły się. Helper/`cap_index_at` nietknięte.

Nie ma kombinacji feature dającej **produkcyjny ID bez pinu**.

### 2. B — pack-audit

`scripts/pack-audit.sh`: drzewo brudne → exit 1; brak manifestu/binarki → exit 1; `src_tree` / `code_tree` / `math_tree` / `sha256` z `release-manifest-testnet.txt` vs `git rev-parse HEAD:…` i `shasum` lokalnego `.so`. Scenariusz „build na A, checkout B, pack” pada na rozjeździe tree lub sha.

Na **mainnet** brakuje analogicznego `release-manifest-mainnet.txt` + `build-mainnet.sh` w tej samej pętli fail-closed oraz kroku „dump on-chain == sha manifestu” w skrypcie (dziś ręczna notatka). To checklista ceremonii, nie dziura w packerze testnetu.

### 3. C — CAPY 20M

`init_capy_vault` (`initialize.rs` ~318–325): `#[cfg(feature = "network-mainnet")] supply == CAPY_TOTAL_SUPPLY` (20e6 × 1e9), błąd `InvalidCapySupply` (koniec enumu). Wcześniej: decimals 9, allowlista, freeze none, **mint authority already None**.

Jednorazowa kontrola wystarcza, bo po wypaleniu authority podaż jest stała.

Ceremonia musi być: **mint dokładnie 20M → disable mint authority → `init_capy_vault`**. Odwrotna kolejność (init przy supply 0, potem mint) jest zablokowana mint-authority checkiem. Testnet (1B) celowo bez tej bramki.

### 4. Regresja

Zgodne z briefem. Delta `src` vs 7.1:

- `lib.rs` — `declare_id` testowy + moduł `program_id_guards`
- `constants.rs` — `CAPY_TOTAL_SUPPLY` + strażnik
- `errors.rs` — `InvalidCapySupply`
- `initialize.rs` — jeden `require!` pod `network-mainnet`

`math_tree` = `6fb61151…` (bez zmian od R6). Settlement, roll, cap, 200M, orphan — nietknięte. Na testnecie nowy wariant błędu istnieje w enumie, ale `require!` podaży jest martwy (`cfg` mainnet).

### 5. Freeze

Tak. Baza mainnetu = ten `src_tree` + ceremonia:

1. `EXPECTED_INIT_AUTHORITY` = multisig (strażnik stringa w tym samym commicie)
2. `cargo-build-sbf --features network-mainnet` (bez `test-periods`)
3. CAPY: supply 20M, authority wypalona, potem `init_capy_vault`
4. upgrade authority → multisig / immutable
5. `pack-audit` + dump vs sha na manifeście **mainnet**

---

## Odhaczenie zadań Kimi (R5 § / R7 §5)

| Punkt | Status |
|---|---|
| Zadanie 1–2 provenance (tree-hash, manifest obok zipa, fail-closed pack) | **done** na ścieżce testnet |
| Zadanie 3 supply-chain / advisory SBF vs host | **done** w R7 (lista); upgrade stosu przed immutable — otwarte, nie ta runda |
| Pin `initialize` + osobny ID buildu bez sieci | **done** |
| Limit 200M / R6-01 / R6-02 | **poza deltą 7.2, zaakceptowane w 7.1** |
| F-02 rozdział kluczy / multisig | **otwarte — ceremonia mainnet, nie freeze testnetu** |
